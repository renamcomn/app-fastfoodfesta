import { Box, Text } from "native-base"

export const Splash = () => {
    return(
        <Box flex={1} justifyContent={'center'} alignItems={'center'}>
            <Text>Splash</Text>
        </Box>
    )
}